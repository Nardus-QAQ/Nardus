#include<stdio.h>
int main(){
    int a,b,c;
    while(scanf("%d%d%d",&a,&b,&c)!=EOF){        //注意while的用法 非常常见！！！！
       if(a>=b&&a>=c)
           printf("%d\n",a);
        else if(b>=a&&b>=c)
            printf("%d\n",b);
        else
            printf("%d\n",c);
    }
}
