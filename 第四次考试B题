/*第四次考试B题*/ 
#include<stdio.h>
int main()
{
	int T; scanf("%d",&T);
	int a,b,c,d,e,k;
	while(T--){ 
	int flag=0; scanf("%d%d",&a,&e);
	for(int i=a;i<=e;i++)
	 {
	 b=i/100;
	 c=(i-b*100)/10;
	 d=i%10;
	 if(i==(b*b*b+c*c*c+d*d*d))
	 printf("%d ",i),flag=1;
	 } 
	 if(flag);
	 puts(""); 
	 if(!flag) puts("-1");
	 }
	 return 0; 
 } //在线做和离线做都一样 他是文件io fstream 
